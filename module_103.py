
def login(user, password):
    if user == "admin" and password == "admin":
        return True
    return False

def login(user, password):
    if user == "admin" and password == "admin":
        return True
    return False

def login(user, password):
    if user == "admin" and password == "admin":
        return True
    return False

# reviewer suggestion

def login(user, password):
    if user == "admin" and password == "admin":
        return True
    return False

class User:
    def __init__(self,name):
        self.name=name
